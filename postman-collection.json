{
  "info": {
    "name": "Task Manager API",
    "description": "Complete API collection for Task Manager",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Create Task",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n    \"title\": \"Complete project documentation\",\n    \"description\": \"Write comprehensive documentation for the Task Manager API\",\n    \"status\": \"pending\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/tasks",
          "host": ["{{base_url}}"],
          "path": ["tasks"]
        }
      }
    },
    {
      "name": "Get All Tasks",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/tasks",
          "host": ["{{base_url}}"],
          "path": ["tasks"]
        }
      }
    },
    {
      "name": "Get Task by ID",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/tasks/1",
          "host": ["{{base_url}}"],
          "path": ["tasks", "1"]
        }
      }
    },
    {
      "name": "Update Task",
      "request": {
        "method": "PUT",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n    \"title\": \"Complete project documentation - Updated\",\n    \"description\": \"Write comprehensive documentation for the Task Manager API - Updated\",\n    \"status\": \"in-progress\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/tasks/1",
          "host": ["{{base_url}}"],
          "path": ["tasks", "1"]
        }
      }
    },
    {
      "name": "Delete Task",
      "request": {
        "method": "DELETE",
        "url": {
          "raw": "{{base_url}}/tasks/1",
          "host": ["{{base_url}}"],
          "path": ["tasks", "1"]
        }
      }
    },
    {
      "name": "Filter Tasks by Status",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/tasks?status=completed",
          "host": ["{{base_url}}"],
          "path": ["tasks"],
          "query": [
            {
              "key": "status",
              "value": "completed"
            }
          ]
        }
      }
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:8080"
    }
  ]
}
